html
    head
        title Productos con PUG
        link(rel='stylesheet' href='https://getbootstrap.com/docs/4.4/dist/css/bootstrap.min.css')
    body 
        div(class="container" style="margin-top: 30px;")
            h3(class="mt-10 text-primary") Estos son todos los productos
            div(class="table-responsive")
                table(class="table table-dark")
                    tr(style="color: rgba(13, 110, 253);")
                        th Titulo 
                        th Precio 
                        th Imagen
                    each pto in data 
                        tr
                            td #{pto.titulo}
                            td $#{pto.precio}
                            td 
                                img(src=`${pto.thumbail}`, alt="", width="40" height="40")
                    else
                        h3 No hay productos guardados

            a(href='/') Agregar un nuevo producto
